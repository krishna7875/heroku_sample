deployment samples 
1 - vercel.com 
    - vercel.json is require 
    - we cant not store data/files[img/pdf/txt] on server[dir] using deployed app {only read con not wrire}--[can store in DB]
    - faster 
2 - render.com 
     - vercel.json is not require 
     - we cant store data/files[img/pdf/txt] on server[dir]
     - slower than vercel